/** concise, functional way to print each argument in console */
args.foreach(arg => println(arg))

/** This will also work, for a function literal that takes a single statement and single argument:
args.foreach(println)
*/
